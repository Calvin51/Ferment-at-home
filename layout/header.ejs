<div class="position-sticky top-0 z-3">
  <nav
    class="navbar navbar-expand-md py-4 bg-white blurred-background"
    style="--bs-bg-opacity: 0.04"
  >
    <div class="container-lg">
      <!-- Logo -->
      <a class="navbar-brand py-0" href="./index.html">
        <img
          src="../assets/images/logo.png"
          alt="Ferment-at-home"
          class="d-none d-lg-block"
        />
        <img
          src="../assets/images/logo-small.png"
          alt="Ferment-at-home"
          class="d-lg-none"
        />
      </a>

      <!-- 手機版漢堡選單 -->
      <button
        class="navbar-toggler border border-2 border-primary rounded-circle p-3"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="text-primary fs-6"><i class="bi bi-list"></i></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <!-- 未登入導覽連結 -->
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 text-center">
          <li class="nav-item me-5">
            <a class="nav-link fw-bold" href="./products.html">商品列表</a>
          </li>
          <li class="nav-item me-5">
            <a class="nav-link fw-bold" href="./about.html">品牌故事</a>
          </li>
          <li class="nav-item me-5">
            <a class="nav-link fw-bold" href="./fqa.html">常見問題</a>
          </li>
        </ul>

        <!-- 未登入狀態 -->
        <div id="auth-block">
          <button
            id="loginBtn"
            class="btn-filled-primary d-none d-lg-block"
            type="button"
          >
            登入/註冊
          </button>
        </div>

        <!-- 已登入狀態 -->
        <div id="member-block" class="dropdown d-none ms-3">
          <a
            class="d-flex align-items-center dropdown-toggle"
            href="#"
            id="memberMenu"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <img
              src="../assets/images/profile-pic-1.svg"
              class="rounded-circle"
              width="32"
              height="32"
              alt="會員頭像"
            />
          </a>
          <ul
            class="dropdown-menu dropdown-menu-end mx-4 my-2"
            aria-labelledby="memberMenu"
          >
            <li>
              <a class="dropdown-item my-3" href="#"
                ><i class="bi bi-person-circle me-1"></i> 個人資料管理</a
              >
            </li>
            <li>
              <a class="dropdown-item my-3" href="#"
                ><i class="bi bi-receipt me-1"></i> 訂單記錄</a
              >
            </li>
            <li>
              <a id="logoutBtn" class="dropdown-item my-3" href="#"
                ><i class="bi bi-box-arrow-right me-1"></i> 登出</a
              >
            </li>
          </ul>
        </div>
      </div>
    </div>
  </nav>
</div>

<!-- Bootstrap Icons -->
<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
/>
<link rel="stylesheet" href="all.css" />
<script>
  const loginBtn = document.getElementById("loginBtn");
  const logoutBtn = document.getElementById("logoutBtn");
  const authBlock = document.getElementById("auth-block");
  const memberBlock = document.getElementById("member-block");

  // 模擬登入
  if (loginBtn) {
    loginBtn.addEventListener("click", () => {
      authBlock.classList.add("d-none");
      memberBlock.classList.remove("d-none");
    });
  }

  // 模擬登出
  if (logoutBtn) {
    logoutBtn.addEventListener("click", () => {
      memberBlock.classList.add("d-none");
      authBlock.classList.remove("d-none");
    });
  }
</script>
